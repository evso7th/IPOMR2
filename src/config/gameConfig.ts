
export const TILE_SIZE = 32; // pixels
export const GRAVITY = 0.5; // pixels per frame^2
export const PLAYER_SPEED = 5; // pixels per frame
export const PLATFORM_SPEED = (PLAYER_SPEED * 0.75) / 2; // pixels per frame for moving platforms
export const JUMP_STRENGTH = -14.14; // pixels per frame (negative is up) - Calculated for ~200px jump height
export const PLAYER_WIDTH = 48; // pixels
export const PLAYER_HEIGHT = 75; // pixels

export const PLAYER_COLOR = 'hsl(var(--primary))'; // Forest Green
export const TILE_COLOR_GROUND = 'hsl(var(--accent))'; // Earthy Brown
export const TILE_COLOR_EMPTY = 'rgba(0,0,0,0)'; // Transparent

